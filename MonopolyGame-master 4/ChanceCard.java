import java.util.Random;

public class ChanceCard {

//generate a random number between 0 and the library length.

Card drawnCard = chanceCards.get()

public void ChanceCardAction(){
    if (Card.action.equals("money")) {
        // Update user's money with card.value
    } 
    else if (Card.action.equals("move")) {
        // Update user's position with card.value
    } 
    else if (Card.action.equals("prison")) {
        // Update user's position; they go to prison
    } 
    else if (drawnCard.action.equals("")) {
        // and so on...
    }
}
    
}
